name: authenticator
description: The implementation of authentication methods for various backends.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ^3.24.0

dependencies:
  auth_client_interface:
    path: ../auth_client_interface
  flutter:
    sdk: flutter
  rxdart: ^0.28.0
  supabase_flutter: ^2.8.4
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^6.0.0
